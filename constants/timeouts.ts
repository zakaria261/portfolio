/**
 * Timeout Configuration Constants
 * Centralized timeout values for consistent behavior across the application
 */

/**
 * Duration (in milliseconds) before resetting form status messages
 * Used after successful submission or errors to clear feedback
 */
export const FORM_STATUS_RESET_DELAY = 5000;

/**
 * Duration (in milliseconds) for API request timeout
 */
export const API_REQUEST_TIMEOUT = 10000;

/**
 * Duration (in milliseconds) for debouncing user input
 */
export const INPUT_DEBOUNCE_DELAY = 300;
